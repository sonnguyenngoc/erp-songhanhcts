<%= form_for([erp_articles, :backend, category]) do |f| %>    
    <div class="row">
        <div class="col-md-6">
            <div class="portlet light bordered">
                <div class="portlet-body">
                    <%= erp_form_control("text", {
                        name: 'category[name]',
                        value: category.name,                   
                        label: t('.name'),
                        placeholder: t('.placeholder_name'),
                        required: true,
                        errors: category.errors.full_messages_for(:name)
                    }) %>
                    <%= erp_form_control("dataselect", {
                        name: 'category[parent_id]',
                        value: category.parent_id,
                        text: category.parent_name,
                        placeholder: t('.placeholder_parent_category'),
                        label: t('.parent_category'),
                        url: erp_articles.dataselect_backend_categories_path(format: 'json'),
                        create: {
                            url: erp_articles.new_backend_category_path,
                            title: t('.create'),
                            container_selector: '.new_category',
                            input_selector: 'input[name="category[name]"]'
                        }
                    }) %>
                    <%= erp_form_control('select', {
                        label: t('.alias'),
                        name: 'category[alias]',
                        value: category.alias,
                        options: Erp::Articles::Category.get_alias_options()
                    }) %>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="portlet light bordered">
                <div class="portlet-body">
                    <%= erp_form_control("textarea", {
                        name: 'category[note]',
                        value: category.note,
                        label: t('.note'),
                        placeholder: t('.placeholder_note'),
                        rows: 2
                    }) %>
                    <%= erp_form_control('checkbox', {
                        name: 'category[disable_submenu]',
                        id: 'category_disable_submenu',
                        value: category.disable_submenu,
                        not_selected_value: 'false',
                        label: t('.submenu'),
                        options: [{value: 'true', text: t('.disable_submenu')}]
                    }) %>
                </div>
            </div>
        </div>
    </div>
    <%= erp_component('button/save', {
        text: t('submit')
    }) %>
    <%= erp_component('button/cancel', {
        text: t('cancel'),
        href: erp_articles.backend_categories_path
    }) %>
<% end %>